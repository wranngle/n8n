{
  "_comment": "Example MCP configuration for local n8n workflow development. Keep real tokens in environment variables or an ignored local config.",
  "mcpServers": {
    "n8n-mcp": {
      "command": "npx",
      "args": ["-y", "@example/n8n-mcp-server"],
      "env": {
        "N8N_API_URL": "https://n8n.example.com",
        "N8N_API_KEY": "${N8N_API_KEY}"
      }
    }
  }
}
