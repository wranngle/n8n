# Shared secret sent as the X-Webhook-Secret header by every n8n webhook caller
# in this repo. The value MUST match the credential bound to each n8n webhook
# node (see docs/WEBHOOK_AUTH.md for the n8n setup steps).
N8N_WEBHOOK_SECRET=
